# Flyfish File Viewer

This sample is rendered entirely in your browser.

- No mandatory server-side conversion
- Local Worker, WASM, font, and vendor assets
- One API for URLs, authenticated `File` objects, and buffers
- Modular presets for lightweight, Office, engineering, or full coverage

Use **Choose a local file** to preview another supported lightweight format.
